% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RT-qPCR_expresion_diferencial.R
\name{RTqPCR_data_analysis}
\alias{RTqPCR_data_analysis}
\title{Pipeline para graficar el ΔΔCt (método de Livak) para datos provenientes del equipo CFX96 (BioRad)'}
\usage{
RTqPCR_data_analysis(
  ruta_rtqpcr,
  numero_replicas = 3,
  resolucion = 600,
  formatos = "jpeg"
)
}
\arguments{
\item{ruta_rtqpcr}{\code{character(1)} Ruta al CSV con columnas mínimas:
\code{Target}, \code{Content}, \code{Sample}, \code{Cq}.}

\item{numero_replicas}{\code{integer(1)} Réplicas a conservar por grupo (se eligen
las más cercanas a la media). Por defecto \code{3}.}

\item{resolucion}{\code{numeric(1)} Resolución (ppi) para dispositivos gráficos al
exportar figuras. Por defecto \code{600}.}

\item{formatos}{\code{character()} Vector de formatos base a exportar (p. ej.
\code{"jpeg"}, \code{"tiff"}, etc.). Deben existir funciones homónimas (\code{jpeg()},
\code{tiff()}, …). Por defecto \code{"jpeg"}.}
}
\value{
(invisible) una lista con:
\itemize{
\item \code{datos_filtrados}: data frame usado para ΔΔCt
\item \code{Fold_Change_df}: resultados por réplica (ΔCt, ΔΔCt, FC)
\item \code{Fold_Change_final}: promedios y desviaciones por grupo
\item \code{ggfold_Livak}: objeto \code{ggplot} del barplot final
}
}
\description{
Lee un CSV con Cq de RT-qPCR (CFX96/CFX Manager), filtra NTC/NA, selecciona las
mejores réplicas por grupo, solicita de forma interactiva el gen de interés,
genes de referencia y condiciones (control y tratamiento), calcula ΔCt/ΔΔCt y
Fold Change (2^-ΔΔCt), genera un gráfico de barras con error y guarda salidas.
}
\details{
La función es \strong{interactiva} (usa \code{readline()}): solicita \code{gen_interes},
\code{gen_referencia} (uno o varios separados por coma o espacio; se promedian),
\code{calibrador} (muestra control) y \code{tratamiento} (muestra tratada).

Escribe en \code{dirname(ruta_rtqpcr)}:
\itemize{
\item \verb{Tabla_Fold_Change_Data_<archivo>_<tratamiento>_<GOI>_<REFS>.csv} (por réplica)
\item \verb{Fold_Change_Data_<archivo>_<tratamiento>_<GOI>_<REFS>.csv} (promedio y sd)
\item \verb{Tabla_filtrada_<archivo>_...csv} (datos usados)
\item Figura \verb{Boxplot_<archivo>.<ext>} y \verb{fold_Livak_<archivo>_<...>.<ext>}
}

\strong{Supuestos de entrada}: el CSV contiene todos los targets y muestras
necesarios. Los NTC con Cq ≥ 31 se marcan como sospechosos; filas con \code{Cq}
faltante se excluyen. Para varias referencias se calcula la media de Cq por réplica.
}
\section{Efectos colaterales}{

Abre dispositivos gráficos base, imprime/guarda figuras y escribe múltiples CSV.
}

\examples{
\dontrun{
RTqPCR_data_analysis("2025/articulo_Rosy/target_genes.csv")
}

}
\seealso{
\link[ggplot2]{ggplot}, \link[psych]{describe}, \link[tidyr]{pivot_wider}
}
